{
  "version": 2,
  "alias": "nexec.n8.io",
  "builds": [
    { "src": "api.sh", "use": "@now/bash" },
    { "src": "package.json", "use": "@now/next" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api.sh" }
  ]
}
